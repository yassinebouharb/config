spring:
  datasource:
    url: jdbc:mysql://mysql:3306/productdb?allowPublicKeyRetrieval=true&useSSL=false&createDatabaseIfNotExist=true
    username:  root
    password:  my-secret-pw
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
